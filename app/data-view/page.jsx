import DataViewPage from '../components/DataViewPage';

export const metadata = {
  title: 'Data View - Certificate Archive',
  description: 'Field, provider, workload, and timeline charts for the certificate archive.',
};

export default function DataView() {
  return <DataViewPage />;
}
